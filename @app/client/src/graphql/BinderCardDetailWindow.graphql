query BinderCardDetailWindow(
  $shortId: String!
  $cardFirst: Int!
  $cardOffset: Int!
  $cardOrderBy: [BinderCardsOrderBy!]!
) {
  binderCardsByShortId(
    binderShortId: $shortId
    first: $cardFirst
    offset: $cardOffset
    orderBy: $cardOrderBy
  ) {
    edges {
      node {
        ...BinderCardDetailFields
      }
    }
  }
}
