query CardsForBinderImport(
  $filter: CardsFilter!
  $first: Int!
  $after: Cursor
) {
  cardsCollection(first: $first, after: $after, filter: $filter) {
    edges {
      node {
        ...CardSearchFields
      }
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}
